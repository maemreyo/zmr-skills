# Brief Version Assertion

Apply the shared `../../_shared/references/brief-version-contract.md` before invoking a specialist. Materialize the assertion and apply `validate_brief_version_assertion()` from `skills.education._shared.contracts`.

The Copilot owns orchestration, not a different version policy. The same stale-input rejection applies when a specialist is invoked directly.
