# Zamery Presentation Contract

Use Zamery Core V2 (`design_system: zamery-core.v2`) and the bundled `classroom-slides-v2` composition contract. The wordmark is lowercase `zamery` with subordinate tagline `rooted in strength`; there is no pictorial logo. Use Arial and the exact palette: ink navy `#17324D`, root terracotta `#B85435`, growth teal `#2B746F`, warm sand `#F4EEE4`, sky mist `#DCEAF2`, charcoal `#202B33`, and paper white `#FFFFFF`. Keep meaning understandable in grayscale and never use color as the sole signal. Reopen the final PPTX after export and rerender every repaired slide.
