# Education V4 operator guide

```bash testable
zamery-v4 --help
```

```bash testable
zamery-v4 graph --help
```

```bash testable
zamery-v4 golden --help
```

```bash testable
zamery-v4 release --help
```

Use plan → run → impact/resume → gate explain → repair → publication. Production publication requires current evidence and approval for the exact graph. Roll back by switching to `v3` with the recorded final V3 tag.
