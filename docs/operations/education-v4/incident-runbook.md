# Incident runbook

For tool outage, stop publication and preserve canonical inputs. For corrupt store/hash mismatch, quarantine the object and rebuild the disposable index. For leaked data, revoke access, rotate credentials, record affected hashes, and run privacy review. For compromised runtime digest, block the capability and invalidate its receipts. For failed canary, switch to the final V3 tag.
