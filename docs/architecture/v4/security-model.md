# Security model

The production default is deny: no network, input-only reads, output-only writes, bounded process resources, pinned runtime identities, safe archive extraction, and redacted observability.
