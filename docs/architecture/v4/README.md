# Education V4 architecture

The V4 pipeline is a content-addressed Python kernel surrounded by isolated capability adapters. Canonical JSON records and blobs are authoritative; SQLite is rebuildable. Evidence, approvals, and gate decisions bind to exact hashes.
